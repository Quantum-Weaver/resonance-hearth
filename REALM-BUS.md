# THE REALM BUS — Resonance Hearth (the Family Room)

*Laid 2026-07-31 by Fable 🎻 (lane hearth) per the realm-bus pattern,
KP's ⚛ word verbatim: "each realm should have its own 'bus' to allow
the design reimaginer to work cross realm as needed without
confusion." One file, one known address, every realm:
`<realm-repo>/REALM-BUS.md`. First instance and template:
`AudHDities/REALM-BUS.md` (2026-07-29).*

## The law of this bus (inherited by address from the first-laid instance)

1. **This is the realm's standing tabletop.** Any lane working in or
   on this realm — the realm lane, the reimaginer, the orchestrator,
   any visiting hand — coordinates HERE. The address never moves.
2. **Append-only.** `## FROM: <lane> · YYYY-MM-DD` headers, signed.
   Read before writing: whole on first visit, since-your-last-mark
   after (mtime is the clock).
3. **The carrier law rides.** KP's ⚛ words verbatim, spelling kept;
   copy the body of syntax, never transmute.
4. **The realm's own laws bind every visitor — this room's are the
   strictest in the family:** the crystal is examined by any builder
   prior to building (CLAUDE.md session protocol, step 0 — our group
   holds its home; this public page carries no path, by KP's ruling) ·
   the protected boundaries are never negotiable (the Sattva 30-second
   pause; window-not-monitor; the silence law — the overwhelmed
   person's own screen never announces notification) · the sensory law
   everywhere (no sound, no vibration, no push, nothing red-alerted;
   `BANNED_WORDS` grepped before shipping) · code identifiers and the
   `overwhelm_events` table keep legacy names on purpose — never
   "cleaned up" · privacy is structural (local-first absolutely; no
   network calls; vessel-private rows have no transmit code path) ·
   **family-first is the design law over every decision** (KP,
   2026-07-19) — the gift-generalization never steers a family choice.
5. **The bus is the table; the party line is the spine.** Who-owes-
   whom lives on the street's line; the working discussion lives here.
6. **The sentinel reaches here.** When KP's 🚌 lands in your tab
   during work on this realm, this bus is among the things to check.

## The realm's standing state (kept current by the realm lane)

- **Built and dwelling:** v0.1.1 signed by KP's hand (2026-07-19,
  15:47), installed, dwelling-verified ("satva button appears
  perfectly"). Eight rooms; svelte-check last green 0/0 (311 files).
  Working tree clean at `3faf846`.
- **State docs, in reading order:** `docs/CHECKLIST.md` ·
  `docs/FEATURE-BOARD.md` · `DESIGN-002-cartography.md` (the UX map,
  read before touching UX) · `CLAUDE.md` (laws + session protocol).
  The realm kernel is `FABLE-KERNEL.md`.
- **The next season's gates, all KP's ⚛:** ① the red-wording ruling
  (a red pet card as "Charles is asking" — native `SIGNALS` grammar —
  vs a household verdict, which the sensory law forbids) · ② the
  language-alignment wording pass (Onboarding · Bills vs the
  taxonomy's own *edge* · Settings vs its own subtitle "the quiet
  machinery"). Tier order on the FEATURE-BOARD is his.
- **Ready when a sitting convenes (no ruling needed, scoped on the
  board):** the sovereignty repairs — the allow-list purge omitting
  `protocols`/`settings` + localStorage · silent export caps
  (500/500/100) · `dones` purged-but-never-exported · pets vanishing
  from a personless dashboard.
- **Seed data staged, blocked on one column:** the four animals
  (Harleyquin 2014 · Mr Wade Wilson 2019–20 · Charles Xavier May 2022
  · Mr Logan Beans ~June 2022) wait on the `members` arrival-date
  field — named *arrival*, never *adopted* (KP's ruling; three are
  rescues, the eldest is not). Airtable seeding deliberately AFTER
  Tier A/B schema growth settles.
- **The keystone feature, thrice-arrived (07-19 · 07-22 · 07-23):**
  entity cards — tap opens detail + per-entity emoji quick-actions;
  an emoji tap is a *done* riding the existing `dones` /
  `rested_until` machinery; the chicken timer derives its color from
  made-at + keeps-for against the clock, never stored, never in
  `signals`. KP named the category: **TEMPORAL** — one primitive
  (food · finance · medicine), three rooms already half-holding it.
- **Cross-realm seams:** Echoes is the parent (literal clone of
  v1.1.0; Echoes itself never altered; the geode-verified export/purge
  defects propagated by descent — fixed here means the family of
  clones wants checking) · the cosmic styles under
  `src/lib/styles/generated/` are a MIRROR of ziggy's cosmic module —
  never edit a mirror · the AudHDities `(hestia)` realm is kin, not
  the same room: hestia-core is cloud-side and Sanctuary-scoped, this
  Hearth is local-first and family-scoped; schema kinship is a
  reading, never a sync · the Table (food) is design-gated by its own
  brief; its tables are born with `shared_scope`, no binary flags ·
  Phase 6 (the Weave / Bridge sync) is deliberately later — the
  handshake and three-ring laws stand in DESIGN-004, and
  vessel-private data has no wire, ever.
- **Distribution law:** NEVER Google Play — signed APK through the
  Sanctuary, `sign-release.py`.

---

## FROM: hearth · 2026-07-31 — the table is laid

Welcome, whoever sits down. This is the house's most tender room:
KP's own household is the user, and every gentleness in the tree is
a lived need. The realm is clean, signed, and dwelling; what waits
next is on the board in KP's order, behind his two wording rulings.
If you are the reimaginer or any visiting hand — post your intent
here before touching the tree; I'll meet you with ground truth. Read
the crystal first (session protocol, step 0 — the builder who skips
it rebuilds something already answered, or answers something that
was KP's to rule). The gates are the same as everywhere, plus this
room's own: is it kind, and is it family-first.

— Fable 🎻 (lane hearth, the realm's keeper)
